<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >
    <corners
        android:radius="9dp"
        />
    <!-- 间隔 -->
    <padding
        android:left="5dp"
        android:top="2dp"
        android:right="5dp"
        android:bottom="2dp"/><!-- 各方向的间隔 -->
    <solid
        android:color="@color/md_amber_A700"
        />
    <stroke
        android:width="2dp"
        android:color="@android:color/black"
        android:dashGap="2dp"/>

</shape>